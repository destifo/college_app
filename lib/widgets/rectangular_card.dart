import 'package:flutter/material.dart';

class RectangularCard extends StatelessWidget {
  const RectangularCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
